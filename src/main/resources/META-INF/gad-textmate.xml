<idea-plugin>
    <!-- Loaded only when the TextMate plugin is available. Provides highlighting
         for the Gad file types by shipping the VS Code grammars as a TextMate
         bundle and delegating the highlighter to TextMate (which resolves the
         grammar by file name). If TextMate is absent, the core plugin still
         works - the Gad files simply render without coloring. -->
    <extensions defaultExtensionNs="com.intellij">
        <lang.syntaxHighlighterFactory language="Gad"
            implementationClass="org.jetbrains.plugins.textmate.language.syntax.highlighting.TextMateSyntaxHighlighterFactory"/>
        <editorHighlighterProvider filetype="Gad"
            implementationClass="org.jetbrains.plugins.textmate.language.syntax.highlighting.TextMateEditorHighlighterProvider"/>
        <editorHighlighterProvider filetype="Gad Template"
            implementationClass="org.jetbrains.plugins.textmate.language.syntax.highlighting.TextMateEditorHighlighterProvider"/>
        <editorHighlighterProvider filetype="Gadx"
            implementationClass="org.jetbrains.plugins.textmate.language.syntax.highlighting.TextMateEditorHighlighterProvider"/>

        <textmate.bundleProvider implementation="dev.gad.intellij.highlight.GadBundleProvider"/>
    </extensions>
</idea-plugin>
