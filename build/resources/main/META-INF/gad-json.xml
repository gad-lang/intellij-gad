<idea-plugin>
    <!-- Loaded only when the JSON module is available (optional dependency). -->
    <extensions defaultExtensionNs="com.intellij">
        <json.jsonSchemaProviderFactory
            implementation="dev.gad.intellij.config.GadJsonSchemaProviderFactory"/>
    </extensions>
</idea-plugin>
